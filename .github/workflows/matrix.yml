name: github actions materix

on: [push]

jobs:
    node-matrix:
        runs-on: ubuntu-latest
        steps:
        - run: node -v
        - uses: actions/setup-node@v6
          with:
            node-version: 18
        - run: node -v